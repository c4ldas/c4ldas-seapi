export const metadata = {
  title: "Chat Commands Transfer",
  description: "Copy chat commands from one channel to another.",
};

export default function Layout({ children }) {
  return (
    <>
      {children}
    </>
  );
}